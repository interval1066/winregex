# winregex

Needed a native win32/64 version of regex so here it is.

Not a fully featured version to be sure, but it is posix-compliant. I needed this to filter input on the windows port of my interp shell application.

